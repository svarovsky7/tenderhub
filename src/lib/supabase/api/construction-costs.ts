import { supabase } from '../client';


  
  try {
      .select('*')
      
    }
    
      
    }
    
    });
    
    });
    
  }

  
  const { data, error } = await supabase
    
  }
  
}

  
  const { error } = await supabase
    .delete()
    .eq('id', id);
    
  }
  
}

  
    
  }
  
}

  
  const { error } = await supabase
    .delete()
    .eq('id', id);
    
  }
  
}

  
  try {
      
    }
    
      
    }
    
      
    }
    
  }
}